key/value colours
key/value padding

tray icons

time colour
date colour

good and bad colours

good and bad thresholds (battery, signal strength)

workspace text extents

font selection

mouse over workspaces

alarm system
mouse over time (set alarm)

vertical nudge

config module order and presence

only do expensive info gathering (fs, if) once every second or so
rather than every frame

modules:
	wan ip
	cpu temp
	cpu usage
	ram usage
	wifi signal strength
	acpi/battery
	alsa volumes
	vpn status
	arp cache watch

