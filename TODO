key/value colours
key/value padding
time colour
date colour
good and bad colours
good and bad thresholds (battery, signal strength, temperature)

tray icons

font selection

alarm system
mouse over time (set alarm)

vertical nudge

config module order and presence

only do expensive info gathering (fs, if) once every second or so
rather than every frame

modules:
	wan ip
	cpu temp
	cpu usage
	ram usage
	wifi signal strength
	alsa volumes
	vpn status
	arp cache watch
	mailbox
	weather
